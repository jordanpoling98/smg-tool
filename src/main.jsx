import React from 'react'
import ReactDOM from 'react-dom/client'
import PromoEmailCopier from './PromoEmailCopier'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PromoEmailCopier />
  </React.StrictMode>,
)
